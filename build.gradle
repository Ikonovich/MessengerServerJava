plugins {
    id 'java'
}

group 'very.serious.productions'
version '0.01-GENEROUS'

repositories {
    mavenCentral()
}
sourceSets {
    main {
        java {
            srcDirs = ['src']
        }
    }
}
dependencies {
    api 'junit:junit:4.12'
    api "mysql:mysql-connector-java:8.0.15"
    api 'org.junit.jupiter:junit-jupiter:5.7.0'
    // https://mvnrepository.com/artifact/com.google.code.gson/gson
    //Below is equivalent to: implementation group: 'com.google.code.gson', name: 'gson', version: '2.7'
    api 'com.google.code.gson:gson:2.7'
}